# Copy this file to .env in each folder as needed

# For lib/db and artifacts/api-server:
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/safeguard_dev

# For artifacts/api-server (optional, defaults to 3000):
PORT=3000

# For artifacts/mobile:
EXPO_PUBLIC_DOMAIN=localhost:3000
